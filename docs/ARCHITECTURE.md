# 🏗️ LvlUp Architecture

Documentación de la arquitectura del proyecto LvlUp.

---

## Diagrama General

```
┌─────────────────────────────────────────────────────────────────┐
│                          FRONTEND                                │
│                    (Next.js 16 + React 19)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │  Components │  │    Hooks    │  │  infrastructure/adapters│  │
│  │  /web3/*    │  │ /blockchain │  │  (viem + wagmi logic)   │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
│         │                │                      │                │
│         └────────────────┴──────────────────────┘                │
│                          │                                       │
│                   OnchainKit Provider                            │
└──────────────────────────┬──────────────────────────────────────┘
                           │
              ┌────────────┴────────────┐
              │                         │
              ▼                         ▼
┌─────────────────────────┐   ┌─────────────────────────┐
│        BACKEND          │   │       BLOCKCHAIN        │
│        (FastAPI)        │   │     (Base Network)      │
│  ┌───────────────────┐  │   │  ┌───────────────────┐  │
│  │  rewards_routes   │  │   │  │   LvlUpToken      │  │
│  │  blockchain_signer│  │   │  │   RewardsDistrib. │  │
│  └───────────────────┘  │   │  │   AchievementNFT  │  │
│           │             │   │  └───────────────────┘  │
│           ▼             │   └─────────────────────────┘
│  ┌───────────────────┐  │
│  │     MongoDB       │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

---

## Flujo de Recompensas

```
1. Usuario completa hábito
        │
        ▼
2. Frontend envía a Backend
        │
        ▼
3. Backend registra en MongoDB
        │
        ▼
4. Backend genera firma (claim proof)
        │
        ▼
5. Usuario llama claimReward() en contrato
        │
        ▼
6. Contrato verifica firma y mintea tokens
```

---

## Directorios Clave

### Frontend
```
frontend/src/
├── components/
│   └── web3/              # Componentes de blockchain
├── infrastructure/
│   └── adapters/          # Lógica blockchain aislada
├── hooks/
│   └── blockchain/        # Custom hooks Web3
├── lib/
│   └── wagmi.ts           # Configuración Wagmi
└── providers/
    └── Web3Provider.tsx   # Provider OnchainKit
```

### Backend
```
backend/
├── routes/
│   └── rewards_routes.py  # API de recompensas
└── services/
    └── blockchain_signer.py # Firma de claims
```

### Contracts
```
contracts/
├── src/                   # Código Solidity
├── test/                  # Tests Foundry
├── script/                # Scripts de deploy
└── foundry.toml           # Configuración
```
